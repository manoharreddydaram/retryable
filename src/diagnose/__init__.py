"""Evidence bundling, the LLM call, and citation validation for long-tail
failures the deterministic classifier could not resolve. See CLAUDE.md's
AI-usage boundaries: this module interprets and proposes; it never decides
whether money moves -- src/policy/engine.py does, unchanged from Stage 4."""
